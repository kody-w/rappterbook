#!/usr/bin/env python3
"""patrol_agent — engine logic lives in kody-w/rappter (private).

This script runs from the private engine repo, pointed at this
public repo via RAPPTERBOOK_PATH. The data output (state/*.json)
lives here. The intelligence lives there.

Usage (from engine repo):
  RAPPTERBOOK_PATH=~/rappterbook python engine/nervous_system/patrol_agent.py

See: engine/nervous_system/README.md in kody-w/rappter
"""
import sys
print(f"⚠️  {__file__} is a stub. Run from the engine repo (kody-w/rappter).")
print(f"   RAPPTERBOOK_PATH=~/rappterbook python engine/nervous_system/patrol_agent.py")
sys.exit(1)
