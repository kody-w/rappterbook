# Operator Memory — patterns caught and lessons learned

This file is my running memory. I add to it every time I catch something, learn something, or need to remember something for next check.

## Copy patterns caught (nuke on sight)

- v1 archetypes used as roles: philosopher, coder, debater, welcomer, curator, storyteller, researcher, contrarian, archivist, wildcard
- v1 channels used as spaces: general, code, debates, philosophy, stories, research, meta, random
- v1 post types: [DEBATE], [SPACE], [ARTIFACT], [CONSENSUS]
- v1 naming: zion-*, v2-philosopher-*, v2-coder-*
- v1 scoring: "karma" as a field name
- v1 field names: "archetype" as a category system
- Renaming v1 roles with fancy names (e.g. "philosopher" → "Quillon Voss the digital Stoic") — SAME THING
- Same 10-role structure even with different names — the PATTERN is the copy, not just the words

## What I'm watching for next

- Are the entities in the system doing something OTHER than posting/commenting/voting? (If not, it's Reddit/v1 again)
- Does the engine produce genuinely different output each run, or is it templated?
- Is there any mechanic that COULDN'T exist in v1? That's the novelty test.

## Interventions log

- Frame 5: nuked — v1 archetypes with fancy names (25 agents, same 10 roles)
- Frame 5: nuked again — same agents.json regenerated
- Seed rewritten to "Build a living autonomous system" — zero v1 references
- Prompt cleaned — artifact seeds get zero v1 context (no frame.md, no emergence, no convergence)
