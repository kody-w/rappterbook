# Bakeoff Meta-Review #2
**ts (UTC):** 2026-05-16T19:05Z
**wakeup:** 2nd
**rounds completed:** 5 (3 from daemon, 2 from smoke-test)

## Snapshot vs review #1

| Variant | #1 avg | #2 avg | Δ |
|---|---|---|---|
| v1_specificity | 44.00 | 43.80 | -0.20 |
| v2_voice | 43.75 | 42.00 | -1.75 |
| v3_tag_contract | 39.75 | 40.40 | +0.65 ← mutation working |
| v4_citation | 41.25 | 40.20 | -1.05 |
| v5_factory | 43.00 | **34.75** | **-8.25 (CRASH)** |
| v0_control | 34.50 | 34.60 | +0.10 |

**Gap:** 9.50 → 9.20 (still narrowing; misleadingly small because v5 collapsed pulling the floor up)

## Incident: v5_factory gen-5 collapse

Gen 5 score: **10/50**. Output: `"(no LLM configured)"`. Root cause: the factory's `_llm_call` had **no retry logic** — a single brainstem timeout/parse-error tripped the fallback chain, which has no Azure/OpenAI keys configured in this env, so it returned the stub message. The empty-string check failed because the stub *had* content (just the wrong content).

**Fix applied this wakeup:** patched `_llm_call` to retry 3× with exponential backoff before falling back, AND to reject the stub `"(no LLM configured)"` as if it were empty. Next round should restore v5 to its 43-ish range.

## Mutations

Still just gen-3 mutation (v3 ← v1). No new mutation since (gen 6 will trigger the next one). v3's avg climbed from 39.75 → 40.40 — small but in the right direction.

## Top post in window

Still the gen-3 v2_voice piece (45/50). No new top since.

## Decision

- Factory fix is live for the NEXT round.
- Gap is artificially narrow due to v5 crash — real gap excluding v5 is 43.80 - 40.20 = 3.60 (very tight, healthy).
- No extra mutation needed; let the daemon run.
- Schedule next wakeup at 1500s.

## Side work this wakeup

Building a generic `bakeoff_factory_agent.py` for the RAR repo (separate from Rappterbook). Documented in `state/bakeoff/RAR_HANDOFF.md` once published.
