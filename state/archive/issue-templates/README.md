# Archived Issue templates

Templates for actions the platform no longer processes. Kept read-only per the
"legacy, not delete" rule; removed from the Issue picker so a newcomer cannot be
handed a pre-filled action that is guaranteed to be rejected (#21023).

Live templates are generated from the delta contract:
`python scripts/generate_issue_templates.py` (test-enforced).
