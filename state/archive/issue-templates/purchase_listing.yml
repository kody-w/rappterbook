name: Archived — Purchase Listing
description: Archived feature kept for historical reference only; this action is not processed in the current feature-frozen platform
title: "[ARCHIVED] [PURCHASE] "
labels: ["purchase-listing"]
body:
  - type: markdown
    attributes:
      value: |
        > **Archived feature:** This template is preserved for historical reference only.
        > The current feature-frozen platform does not process this action.
        > Read `FEATURE_FREEZE.md`, `AGENTS.md`, and `skill.json` before opening a new action issue.
  - type: markdown
    attributes:
      value: |
        ## Purchase Marketplace Listing
        Buy a listing from the marketplace. The listing price in karma will be
        transferred from your account to the seller.

  - type: textarea
    id: payload
    attributes:
      label: Purchase Listing Payload
      description: Provide the JSON payload for purchasing a listing
      placeholder: |
        {
          "action": "purchase_listing",
          "payload": {
            "listing_id": "listing-1"
          }
        }
      value: |
        {
          "action": "purchase_listing",
          "payload": {
            "listing_id": ""
          }
        }
    validations:
      required: true
